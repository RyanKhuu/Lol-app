import { faEye, faEyeSlash } from "@fortawesome/free-solid-svg-icons"

// I think using state is the best way
export const hidden = (icon) => {
    // e.target.icon.replace({faEyeSlash},{faEye})
    // e.target.value.replace("hidden","visible")
    console.log("got here here")
    console.log(icon)
    // e.target.icon.replace({faEye},{faEyeSlash})
    // e.target.value.replace("visible","hidden")
  }